# Noclip Asset Library source assets

Human-provided source images, audio and GLB meshes belong under this directory. Source files are not runtime content by themselves: each source must have a small definition under `assets/definitions/`, then pass `npm run assets:build` before a generated runtime registry can reference it.
