import { readFileSync } from 'node:fs';
import { pathToFileURL } from 'node:url';
import { resolve } from 'node:path';
const ROOT=process.cwd();const compiled=(path)=>pathToFileURL(resolve(ROOT,'.studio-dist',path)).href;const input=JSON.parse(readFileSync(0,'utf8')||'{}');const action=process.argv[2];
async function context(payload){const contexts=await import(compiled('src/presentation/developmentContext.js'));const exports=await import(compiled('src/presentation/developmentContextExports.js'));const ctx=contexts.developmentContextForDesignTargetId(payload.targetId,payload.options??{});if(!ctx)throw new Error(`Unknown DevelopmentContext target ${payload.targetId}`);return{context:ctx,json:exports.developmentContextJson(ctx),human:exports.formatDevelopmentContext(ctx,payload.mode??'CHANGE'),fullPrompt:exports.fullDevelopmentPrompt(ctx,payload.mode??'CHANGE')};}
async function main(){if(action==='context')return context(input);if(action==='context-packet'){const exports=await import(compiled('src/presentation/developmentContextExports.js'));return{context:input.context,json:exports.developmentContextJson(input.context),human:exports.formatDevelopmentContext(input.context,input.mode??'CHANGE'),fullPrompt:exports.fullDevelopmentPrompt(input.context,input.mode??'CHANGE')};}if(action==='targets'){const module=await import(compiled('src/presentation/studioTargets.js'));return module.STUDIO_TARGETS;}if(action==='profiles'){const module=await import(compiled('src/presentation/assets.js'));return module.ASSET_PROFILES;}if(action==='registry'){const module=await import(compiled('src/presentation/projectPresentationRegistry.js'));return module.PROJECT_PRESENTATION_REGISTRY;}if(action==='receipt'){const receipts=await import(compiled('src/presentation/changeReceipt.js'));const exports=await import(compiled('src/presentation/changeReceiptExports.js'));const receipt=receipts.createChangeReceipt(input.context,input.change);return{receipt,json:exports.changeReceiptJson(receipt),human:exports.formatChangeReceipt(receipt)};}throw new Error(`Unknown canonical action ${String(action)}`);}
try{process.stdout.write(`${JSON.stringify(await main())}\n`);}catch(error){console.error(error instanceof Error?error.message:String(error));process.exit(1);}
