{
  "name": "project-noclip",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "A deterministic browser-first living Backrooms simulation.",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "build:local": "node scripts/build-local.mjs",
    "preview": "node scripts/preview.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node scripts/test.mjs",
    "check": "npm run typecheck && npm test && npm run benchmark && npm run build",
    "benchmark": "node scripts/benchmark.mjs",
    "fields:lab": "node scripts/field-lab.mjs"
  },
  "dependencies": { "playcanvas": "2.21.3" },
  "devDependencies": { "typescript": "^5.8.3", "vite": "^7.1.0" },
  "engines": { "node": ">=22.12.0" }
}