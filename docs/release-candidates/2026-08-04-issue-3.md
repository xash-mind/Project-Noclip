# Issue #3 Browser Release Candidate

**Runtime base source:** `e20237576794c12e4288e9c94d400048f4cb679f`  
**Verified browser-test head:** `38ccb89b05dd7a624a91ae6579a0e25dd3b1e49a`  
**Branch:** `agent/issue-3-browser-release-candidate`  
**GitHub Actions run:** `30916882993`  
**Browser artifact:** `8895476174`  
**READY Vercel preview:** `dpl_Fge5msPa3QQFQXDgqUCkRpgo12ZP` from commit `76b75218b649652b1ba0d81b920361a5880a4467`

## Source and artifact parity

The READY Vercel preview and the passing browser-test head contain the same application runtime. The only changes from `76b75218` to `38ccb89b` are the GitHub Actions workflow and browser-smoke test files. No file under `src/`, no package input, no Vite configuration and no persistence/world data contract changed.

The CI production build and Vercel preview both produced:

- 1,230 transformed modules
- `index-BAgjfvKl.css`
- `index-DhjFZXOY.js`
- JavaScript size approximately 1,989.25 kB minified / 516.74 kB gzip

## Verification result

GitHub Actions run 28 passed:

- strict TypeScript
- 13 deterministic/system tests
- 10,000-cell generator benchmark with zero connector errors and zero placement errors
- production Vite build
- headless Chromium 150 journey smoke with software WebGL

The browser smoke confirmed:

- fresh title screen and **Begin new local journey** action
- PlayCanvas/WebGL startup without the former fog exception
- textured Level 0 rendering, fluorescent lighting, HUD and timeline watch
- six inventory slots
- IndexedDB save schema v2 creation
- pointer lock granted to the game canvas
- World Lab diagnostics
- direct refresh exposing Continue
- restoration of the same character and seed
- no blocking browser-console errors

Initial browser baseline:

- loaded cells: 49
- colliders: 550
- interactions: 7
- draw calls: 462
- used JavaScript heap: approximately 24.1 MB
- total JavaScript heap: approximately 54.2 MB

## Remaining manual regression coverage

Headless Chrome granted pointer lock, but synthetic `KeyW` events did not move the player. Native keyboard movement remains a manual browser check; movement has existing player feedback and deterministic collision coverage, but the CI report does not claim native input automation.

Notes, marker drawing and glow-stick interactions also remain manual journey checks. Their presence in source or a prior specification is not treated as fresh browser evidence here.

## Promotion rule

Promote only the application runtime represented by the READY preview and passing browser build. Test-only or documentation-only commits may differ while the application runtime remains byte-identical. Any future runtime change requires a replacement preview and browser run before production promotion.
