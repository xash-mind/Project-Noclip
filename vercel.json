{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "cleanUrls": true,
  "ignoreCommand": "git fetch --depth=1 origin \"${VERCEL_GIT_PREVIOUS_SHA:-HEAD^}\" >/dev/null 2>&1 || true; git diff --quiet \"${VERCEL_GIT_PREVIOUS_SHA:-HEAD^}\" HEAD -- src package.json package-lock.json VERSION vite.config.ts index.html vercel.json scripts/build.mjs scripts/preview.mjs || exit 1",
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
