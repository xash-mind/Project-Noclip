import {spawnSync} from 'node:child_process';try{const vite=await import('vite');await vite.build();}catch(error){const missing=error&&typeof error==='object'&&'code'in error&&error.code==='ERR_MODULE_NOT_FOUND';if(!missing)throw error;console.warn('Vite unavailable; using TypeScript static fallback with PlayCanvas import map.');const result=spawnSync(process.execPath,['scripts/build-local.mjs'],{stdio:'inherit'});if(result.status!==0)process.exit(result.status??1);}
